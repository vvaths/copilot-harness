# Project Services & Commands
# This file is read by agents to discover how to build, test, and run services.
# Update this file when adding new services or changing ports.

# Customize these commands for your project's tech stack:
commands:
  install: npm install
  test: npm test
  build: npm run build
  # lint: npm run lint
  # typecheck: npm run typecheck

# Define your services here. Example:
# services:
#   app:
#     start: npm run dev
#     stop: lsof -ti :3000 | xargs kill
#     healthcheck: curl -sf http://localhost:3000/health
#     port: 3000
#     depends_on: []
