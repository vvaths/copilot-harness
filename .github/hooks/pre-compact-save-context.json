{
  "hooks": {
    "PreCompact": [
      {
        "type": "command",
        "command": "node -e \"const fs=require('fs');const exists=f=>{try{fs.accessSync(f);return true}catch(e){return false}};const files=['docs/validation-contract.md','docs/features.json','docs/services.yaml','AGENTS.md'].filter(exists);if(files.length>0){process.stdout.write(JSON.stringify({systemMessage:'Context compacting. After compaction, re-read: '+files.join(', ')}))}\"",
        "timeout": 10
      }
    ]
  }
}
