{
  "hooks": {
    "Stop": [
      {
        "type": "command",
        "command": "node -e \"const input=JSON.parse(require('fs').readFileSync(0,'utf8')); if(input.stop_hook_active){process.exit(0)} const fs=require('fs'); const contract='docs/validation-contract.md'; if(!fs.existsSync(contract)){process.stdout.write(JSON.stringify({hookSpecificOutput:{hookEventName:'Stop',decision:'block',reason:'Validation contract (docs/validation-contract.md) does not exist. Define the validation contract before finishing.'}})); process.exit(0)} process.exit(0)\"",
        "timeout": 15
      }
    ]
  }
}
